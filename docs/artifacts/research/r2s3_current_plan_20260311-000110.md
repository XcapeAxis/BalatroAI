# R2-S3 当前计划

- 时间：`20260311-000110`
- 分支：`lab/research-r1`
- worktree：`D:\MYFILES\BalatroAI_r1`

## 当前已确认事实
1. `legality parity` 已修复，当前评测协议比 R1 初期可信。
2. `reward_survival` 仍是纯 RL 的可信起点与对照基线。
3. `balanced hard-case replay` 已证明有正收益。
4. `flat self-imitation` 已经证伪；它不能再作为默认 recipe。
5. `stage-aware curriculum + reward schedule + late-stage low-ratio self-imitation` 是当前 strongest certified pure-RL recipe。
6. 当前 strongest certified pure-RL checkpoint 是 `rl_policy:p42_rl_candidate:r2s2-cert-c3-20260310-2338-candidate-rl:aaaaaaa,bbbbbbb,ccccccc,ddddddd:1cbb404ba5`。
7. 统一 compare 下该 strongest recipe 的均分是 `96.5`，heuristic champion 是 `303.0`，当前 gap 是 `206.5`。
8. 当前主要瓶颈不是 legality，也不是是否切 warm-start，而是：
   - failure coverage 仍然过窄；
   - replay source 结构太粗；
   - 训练阶段缺少 per-bucket / per-slice 反馈；
   - 当前 mining 结果几乎被 `early_collapse` 吞掉，真实失败原因没有被拆开。

## 今夜候选方向排序
| 排名 | 方向 | 预期收益 | 改动成本 | 认证可验证性 | 风险 | 结论 |
|---|---|---:|---:|---:|---:|---|
| 1 | Candidate A — 扩大 failure bucket 覆盖 | 高 | 中 | 高 | 低 | 今夜优先入口。先把 failure taxonomy 从“结果标签”改成“更接近原因的标签”。 |
| 2 | Candidate B — bucket-aware replay 配比优化 | 高 | 中 | 高 | 中 | 必须紧跟 A，否则 taxonomy 只是文档改进，不会真正改变训练。 |
| 3 | Candidate C — slice-targeted replay | 中高 | 中 | 高 | 中 | 在 A/B 打通后立刻做，优先针对 `discard/resource_pressure/early` 等 recurring pain points。 |
| 4 | Candidate E — reward schedule / curriculum 联动细化 | 中 | 中 | 中高 | 中 | 应建立在 replay/source 已结构化之后，否则只是继续盲调 reward。 |
| 5 | Candidate D — 受控 self-imitation 精修 | 中低 | 中 | 中 | 中 | 仅保留为晚阶段小比例增益项，不再作为主增益来源。 |
| 6 | Candidate F — 训练吞吐/采样效率微优化 | 低 | 中高 | 中 | 高 | 只有在明确证明吞吐已成为瓶颈时才做，今晚默认不做。 |

## 今夜第一推进方向
选择：**Candidate A + Candidate B 组合起手**。

原因：
- 当前最清楚的问题不是“权重不对”，而是失败样本被粗粒度聚合，导致 replay 再怎么调也只是在放大 `early_collapse`。
- 先把 failure taxonomy 和 replay mixer 做成 bucket-aware，才能让后续 curriculum、slice-targeted replay、受控 self-imitation 有真实控制面。
- 这条路径的验证成本低，且能直接用 closed-loop smoke + certification compare 验证。

## 今夜批次计划
### Batch 1
- 目标：扩展 failure taxonomy，改变 bucket 判定优先级，让更具体的失败原因先于 `early_collapse` 暴露。
- 验收：`failure_bucket_coverage` 明显高于 R2-S2；`failure_bucket_stats.json` 不再只剩一个 dominant bucket。

### Batch 2
- 目标：把 `slice/risk-aware replay` 真正接进 PPO 采样计划。
- 验收：`bucket_replay_stats.json` / `slice_metrics.json` 能反映目标 mix，且训练路径真实使用这些字段。

### Batch 3
- 目标：把 self-imitation 约束到后期、低比例、allowlist 控制，并用 rollout episode metadata 做过滤。
- 验收：`self_imitation_stats.json` 能清楚说明启用条件、allowlist、实际命中和比例。

### Batch 4
- 目标：基于新的 bucket/slice mix 做 curriculum + reward schedule 联动精修，形成 strongest recipe 候选。
- 验收：closed-loop compare 中至少有一个新 recipe 稳定优于 `reward_survival_baseline` 或 `r2s2_bucket_curriculum_cert` 之一。

### Certification C4
- 比较对象至少包含：
  1. `heuristic_baseline`
  2. `reward_survival_baseline`
  3. `r2s2_bucket_curriculum_cert`
  4. `r2s3` strongest bucket-aware recipe
  5. `r2s3` strongest bucket-aware + controlled self-imitation recipe（若成立）
- 验收：形成新的 strongest certified pure-RL recipe，或者明确证明本轮改造没有越过 R2-S2 certified baseline。

## 今夜停止条件
- 只有触发 human gate / hard block，或完成 **4 个 batch + 1 次认证 compare + scoreboard/registry/summary 全更新 + 晨报刷新** 才允许停。

## 可能触发 human gate 的点
- 若某些关键 failure bucket 明显来自 sim / 机制误差，而本地资料不足以判断，就进入机制研究并必要时上网查证。
- 若统计差异非常接近，且会显著影响是否继续纯 RL 主线，就写 attention item 而不是越权决策。
